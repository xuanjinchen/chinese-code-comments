---
name: chinese-code-comments
description: 在创建、修改、重构或修复任意语言代码时，添加、更新和审核准确、耐维护的代码注释；也用于用户明确要求添加、更新或审查代码注释，以及提出中文注释、指定语言注释、逐行注释、代码块注释、方法、类或 API 文档注释等要求。默认使用简体中文，用户指定语言或项目规范优先。任何语言的逐行等价请求（如 English “line-by-line”、日文“一行ずつ”）只要未包含“每一行都必须”“每条都必须”等显式全称约束，一律按语义块使用 GROUPED；STRICT 仅用于显式全称约束。产生代码写入的任务强制使用两阶段完整 diff 流程；纯只读解释或普通代码审查均不强制。
---

# Chinese Code Comments

## 解析本次注释策略

1. 先读取用户要求、当前目录最近的项目规范、现有代码与测试，并判断任务是否产生代码写入。
2. 先确定并锁定注释模式，后续不得重新解释：
   - `STRICT`：当且仅当用户以任何语言正向、显式提出全称约束，例如“每一行都必须注释”“每条可执行语句都必须独立注释”、English “every executable line must”或日文“すべての実行行に必ず”；“不要每行都注释”等否定式提及不能触发 `STRICT`。
   - `GROUPED`：当且仅当请求正向提出任何语言的逐行等价要求、且未包含上述全称约束时，按语义块处理；例如中文“逐行注释”、English “line-by-line comments”和日文“一行ずつコメント”都属于此模式。缺少全称约束本身不是 `GROUPED` 触发条件；普通“添加注释”“补充说明”“更新失真注释”等请求保持 `SCOPED`。“不逐行”“没有逐行要求”等否定式提及也不能触发 `GROUPED`。
   - `SCOPED`：其他请求按用户指定、项目就近规范或默认高价值维护注释处理；不产生注释写入的纯只读代码审查也使用 `SCOPED`。
3. 收到逐行类请求时，在编辑或输出代码前先用一句话说明已选择 `GROUPED` 或 `STRICT` 及触发依据。用户要求只返回代码时可以省略对外说明，但仍要先在工作记录中固定模式。
4. 按“用户明确指定、项目就近规范、简体中文”确定新增或更新注释的语言。
5. 按“用户明确指定、项目就近规范、默认高价值维护注释”确定范围、粒度和格式；这里的用户优先级不得推翻第 2 步的逐行模式分类。
6. 保留准确的现有注释及其语言；只有注释失真、错误或与改动冲突时才更新。
7. 使用目标语言和项目惯用的行注释、块注释、docstring、Javadoc、TSDoc 或等价格式。

## 仅对代码写入任务分两个阶段工作

产生代码写入的任务必须在实现过程中记录关键意图，并在结束前审查完整改动。纯只读的代码解释或代码审查不强制执行实现阶段或完整 diff 流程；只有用户明确要求审核注释时，才在只读范围内检查现有注释，不虚构改动。

### 实现过程中

- 在实现复杂业务规则、边界保护、异常恢复、并发控制、事务、幂等、资源清理或兼容逻辑时，同步记录关键意图。
- 默认解释业务意图、约束、边界、异常、并发、资源管理、兼容性和非直观实现。
- 容量为终止符或哨兵保留空间、失败路径释放资源、成功后转移所有权都属于跨调用契约；需求或代码存在这些约束时，即使单行实现直观，也要在源码最相关位置记录。约束分散在不同分支或转移点时必须分别贴近记录，不能合并成远离失败分支或所有权转移点的一条注释，也不能只在最终回复中概述。
- 不猜测无法从需求、代码、测试、文档或已确认 skill 结论验证的业务事实。
- 不逐句翻译变量赋值、普通循环或显而易见的函数调用，除非用户明确要求对应粒度。

### 结束前审查完整改动

1. 获取本次完整改动。Git 项目检查 diff 和未跟踪文件；非 Git 项目检查本次实际触及的文件。
2. 结合周边实现、测试和文档复核意图，不能只阅读新增行。
3. 查找缺失、重复、显而易见、错误和已经失真的注释。
4. 按本次语言和粒度策略新增、更新或删除注释，只处理与本次改动有关的范围。
5. 确认注释不得改变代码逻辑、破坏语法、泄露秘密或承诺未经验证的行为。
6. 运行与改动风险相称的格式化、语法检查或既定测试；已经完成的验证不无理由重复执行。
7. 在最终回复中报告注释审查结果。没有维护上下文需要记录时，明确说明已检查且不需要新增注释。

## 遵循用户指定的注释粒度

- 支持逐行注释、代码块注释、方法或函数注释、类注释、API 文档注释、参数、返回值、异常和其他自定义基准。
- `GROUPED` 模式先把每个函数划分为少量逻辑步骤，把连续且语义一致的代码作为一个块；一条注释原则上覆盖至少两条相关可执行语句，不单独注释函数声明、显而易见赋值或简单返回。
- `STRICT` 模式为每条可执行语句分别注释，同时保持代码可编译、可解析和可格式化；`else`、`catch`、`finally` 等不代表独立可执行语句的结构行不单独添加注释。
- 用户指定关注点时，只围绕该关注点补充信息，不额外扩张注释范围。
- 结束前按已锁定模式复核：`GROUPED` 若几乎每条语句都有独立注释，必须合并连续的单语句注释；`STRICT` 若存在未覆盖的可执行语句，必须补齐。

## 按需联动相关 skill

本 skill 只负责生成、更新和审查代码注释。辅助 skill 仍按各自触发条件工作，其结论只作为理解注释意图的输入，不扩展本 skill 的职责。

- 遇到 Bug、失败测试或异常行为且已触发 `systematic-debugging` 时，复用其根因、触发条件和防回归结论。
- 完成功能、高风险修改或合并前检查且已触发 `requesting-code-review` 时，复用其识别的风险、约束和回归面。
- 涉及公共 API、复杂业务、架构或维护说明且已触发 `technical-writer` 时，复用其领域术语、契约和读者所需上下文。
- 前端、React、移动端、架构、测试和其他技术栈仅在对应 skill 的正常触发条件满足时联动。
- 不为增加注释而强制触发不适用的辅助 skill；不重复执行已有分析，也不把冗长结论直接复制到源码。

## 处理文件与格式边界

- 默认不修改标准 JSON、锁文件、生成代码、第三方依赖、压缩文件或不支持注释的格式。
- 用户要求的目标格式不支持注释时，不写入非法内容；根据任务范围在相邻代码、文档或最终回复中说明。
- 无法确认业务意图且错误注释风险较高时，不在源码中猜测，并在最终回复指出缺失信息。

## 保持注释可维护

- 解释稳定的原因、不变量、契约和风险，而不是记录聊天过程或短期实现历史。
- 使用最短但足以防止误解的表达，把注释放在相关声明、分支或代码块附近。
- 不用注释替代清晰命名、必要重构或正确测试。
- 代码行为变化时同步更新注释；注释失真时删除或重写。
